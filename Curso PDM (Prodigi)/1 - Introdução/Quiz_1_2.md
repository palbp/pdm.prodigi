# Perguntas 1.2

1. O ficheiro APK que contém a a aplicação a ser instalada nos dispositivos inclui:  
    - Apenas o código fonte em Kotlin
    - Apenas o código compilado  
    - O código compilado, os recursos da aplicação e o AndroidManifest  
    - Apenas os recursos da aplicação

2. A seguinte afirmação é verdadeira ou falsa? Os valores de minSdk e targetSdk definidos no ficheiro de build do módulo são refletidos no elemento uses-sdk do ficheiro de manifesto final, permitindo validar a instalação no dispositivo.   
  
3. Quais destes componentes Android podem atuar como pontos de entrada da aplicação?   
    - Apenas Activities  
    - Apenas Services  
    - Apenas Broadcast Receivers e Content Providers
    - Activities, Services, Broadcast Receivers e Content Providers